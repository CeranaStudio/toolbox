# Groq API 金鑰，從 https://console.groq.com 取得
GROQ_API_KEY=

# --- 選填 ---

# 每個音頻片段時長（秒），預設 600（10 分鐘）
# 如果音頻超過 25MB 限制，請調低
# SEGMENT_TIME_SECONDS=600

# API 請求間延遲（秒），避免 rate limit，預設 2
# API_REQUEST_DELAY=2
